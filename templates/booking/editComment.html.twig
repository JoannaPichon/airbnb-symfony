{% extends 'base.html.twig' %}

{% block title %}Modifier le commentaire{% endblock %}


{% block body %}
	{# <div class="container mt-3">
		<div class="row">
				<div class="row m-4 mx-auto" id="commentEdit" >
					<div class="col">
						<h3 class="text-center">Votre avis sur le séjour</h3>

						{{form_start(form)}}

						{{form_row(form.rating, {'label': 'Note sur 5'})}}
						{{form_row(form.content, {'label': 'Votre commentaire sur le séjour'})}}
						<button type="submit" class="btn btn-primary">Poster</button>

						{{form_end(form)}}
					</div>
				</div>
			</div>
		</div>
	</div> #}
{% endblock %}